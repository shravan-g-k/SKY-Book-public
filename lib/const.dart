import 'package:journalbot/private.dart';
// Used to get the server address deployed on localhost
// ipv4 is the IP address of the machine running the server(my computer)
const serverAddress = "http://$ipv4:3000";

const String tokenKey = "token";